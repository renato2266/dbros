<%= form_for(@area) do |f| %>
  <% if @area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% @area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :area_criminale %><br>
    <%= f.text_field :area_criminale %>
  </div>
  <div class="field">
    <%= f.label :note_area %><br>
    <%= f.text_area :note_area %>
  </div>
  <div class="field">
    <%= f.label :ambit_id %><br>
    <%= f.collection_select :ambit_id, Ambit.all, :id, :ambito_criminale, :include_blank=>true, :prompt=>'Please select ambit' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
