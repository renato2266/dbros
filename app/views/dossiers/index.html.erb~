<%- model_class = Dossier -%>
<div class="page-header">
  <h3><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h3>

<p align="right"><%= link_to t('.new', :default => t("helpers.links.new")),
            new_dossier_path,
            :class => 'btn btn-primary' %>
</p>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:numero) %></th>
      <th><%= model_class.human_attribute_name(:data) %></th>
      <th><%= model_class.human_attribute_name(:oggetto) %></th>
      <th><%= model_class.human_attribute_name(:documento_file_name) %></th>

    </tr>
  </thead>
  <tbody>
    <% @dossiers.each do |dossier| %>
      <tr>
        <td><%= dossier.numero %></td>
        <td><%= dossier.data %></td>
        <td><%= dossier.oggetto %></td>
        <td><%= link_to dossier.documento_file_name, dossier.documento.url%></td>
        <td>


          <%= link_to t('.show', :default => t("helpers.links.show")),
                      show_dossier_path(dossier), :class => 'btn btn-mini' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_dossier_path(dossier), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      dossier_path(dossier),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


