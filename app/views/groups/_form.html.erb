<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gruppo_criminale %><br>
    <%= f.text_field :gruppo_criminale %>
  </div>
  <div class="field">
    <%= f.label :note_gruppo %><br>
    <%= f.text_area :note_gruppo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
