<%= form_for(@fascicle) do |f| %>
  <% if @fascicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fascicle.errors.count, "error") %> prohibited this fascicle from being saved:</h2>

      <ul>
      <% @fascicle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %><br>
    <%= f.number_field :numero %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>

	<div class="field">
    <%= f.label :name %>
	<%= f.collection_select :name_ids, Name.all, :id, :cognome_nome, :include_blank=>true, :multiple => true %>
	</div>

	<div class="field">
    <%= f.label :group %>
	<%= f.collection_select :group_ids, Group.all, :id, :gruppo_criminale, :include_blank=>true, :multiple => true %>
	</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
